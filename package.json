{
  "name": "chainvote",
  "version": "1.0.0",
  "description": "Blockchain-based voting platform with face recognition",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "setup": "npm run install:all && cd server && npm run db:init"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
